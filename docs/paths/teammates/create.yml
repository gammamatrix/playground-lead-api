get:
  tags:
    - Teammate
  summary: 'Create a teammate form.'
  operationId: create_teammate
  responses:
    200:
      description: 'The create teammate information.'
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: ../../models/teammate.yml
              meta:
                type: object
    401:
      description: Unauthorized
    403:
      description: Forbidden
