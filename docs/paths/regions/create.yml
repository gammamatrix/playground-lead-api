get:
  tags:
    - Region
  summary: 'Create a region form.'
  operationId: create_region
  responses:
    200:
      description: 'The create region information.'
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                $ref: ../../models/region.yml
              meta:
                type: object
    401:
      description: Unauthorized
    403:
      description: Forbidden
